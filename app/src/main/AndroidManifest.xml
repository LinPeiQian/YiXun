<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chwings.letgotips">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".LGApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity"></activity>
        <activity android:name=".testCase.TestZhuJieActivity" />
        <activity android:name=".activity.BaseActivity" />
        <activity android:name=".testCase.TestHttpActivity" />
        <activity android:name=".activity.guide.HighQualityTravelActivity" />
        <activity android:name=".activity.guide.TravelDetailedActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.guide.TravelDetailed"></activity>
    </application>

</manifest>